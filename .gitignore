# Brain runtime artifacts (never source)
server/.venv/
server/__pycache__/
server/**/__pycache__/
server/data/
server/llm_logs/
server/logs/
server/uploads/
*.pyc
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
.env
.DS_Store

# 部署系统（agent-control-plane-deployment）在 runtime 目录里的产物：
# build.sh 的打包输出、平台写入的版本文件、只保留 backend/ 的运行期目录。
outputs/
backend/
VERSION
COMMIT
DEPLOYMENT
GIT_REPO_URL
